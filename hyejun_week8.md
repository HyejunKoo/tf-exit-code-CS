# Cloud / DevOps Project & Linux Interview

---

## 제출 기준

- 필수 답변: ROLE-001 ~ ROLE-020
- 선택 답변: ROLE-021 ~ ROLE-030

---

## 필수 질문

## [ROLE-001] 리눅스 서버를 재부팅했지만 서비스가 정상적으로 올라오지 않는 경우, 어떤 순서로 원인을 확인할지 설명해 주세요.

답변: 먼저 서버 자체가 정상 부팅됐고 SSH 접속이 가능한지 확인한다. 이후 systemctl status 서비스명으로 서비스의 현재 상태와 최근 로그를 확인해 active, inactive, failed 중 어떤 상태인지와 종료 코드를 본다. systemctl is-enabled 서비스명 은 부팅 시 자동 실행 설정을 확인하는 명령으로, 결과가 disabled라면 재부팅 후 서비스가 자동으로 시작되지 않은 것. 서비스가 시작에 실패했다면 journal -u 서비스명 -b로 이번 부팅 이후 해당 서비스의 로그만 조회해 설정 오류, 권한 문제, 환경변수 누락, 포트 충돌, 외부 연결 실패 등의 원인을 찾는다.다음으로 ps는 서비스 프로세스가 실제로 실행 중인지, ss -Intp는 애플리케이션 포트가 열려 있고 어떤 프로세스가 점유하는지를 확인하는 데 사용
재부팅 이후 문제이므로 systemctl cat 서비스명으로 After, Requires, Environment 설정을 확인해 네트워크, 마운트, DB같은 의존성이 준비되기 전에 서비스가 실행됐거나 필요한 환경변수가 누락되지 않았는지도 점검합니다. 조치 후 서비스를 재시작하고 curl localhost:포트/health로 서버 내부에서 애플리케이션 응답을 확인한 뒤, 외부 도메인으로도 요청해 프록시나 로드밸런서까지 전체 경로가 정상인지 검증

참고 자료:

---

## [ROLE-002] 리눅스에서 `grep` 명령어를 실행하면 쉘에 명령어를 입력한 순간부터 결과가 출력되기까지 내부적으로 어떤 과정이 일어나는지 설명해 주세요.

답변: 쉘이 명령어와 인자를 파악하고 실행 파일을 찾은 뒤 일반적으로 자식 프로세스를 만들고 exec 계열 시스템 콜로 grep 프로그램을 적재한다. 

참고 자료:

---

## [ROLE-003] 리눅스에서 프로세스와 스레드의 차이를 설명하고, 장애 분석 시 각각을 어떻게 확인할 수 있는지 설명해 주세요.

답변: 프로세스는 독립된 가상 주소 공간과 자원을 가진 실행 단위이고, 스레드는 한 프로세스 안에서 코드, 힙, 파일 디스크립터 등을 공유하는 실행 단위이다. 프로세스 간 격리는 강하지만 전환 비용이 상대적으로 크고, 스레드는 통신이 빠른 대신 동기화 문제에 주의해야 한다. 장애 시에는 ps aux나 top으로 프로세스 별 자원 사용량을 보고, top -H나 ps -eLf로 특정 프로세스 안의 스레드가 CPU를 독접하는지 확인한다.

참고 자료:

---

## [ROLE-004] 서버 CPU 사용률과 Load Average가 갑자기 증가했을 때 `top`, `ps` 등의 명령어를 사용해 원인을 좁히는 과정을 설명해 주세요.

답변: 우선 top에서 전체 CPU사용률과 user, system, iowait 비중을 구분하고 Load Average를 cpu 코어 수와 비교. load average에는 cpu를 기다리는 작업뿐 아니라 중단 불가능한 I/O 대기 작업도 포함되므로 CPU문제라고 바로 단정하지 않는다. 이후 ps나 pistat로 점유율이 높은 PID와 스레드를 찾고 로그, 배포 이력, 요청량을 같은 시간대로 비교. I/O 대기가 높다면 디스크나 네트워크 쪽으로 조사 범위 전환

참고 자료:

---

## [ROLE-005] 리눅스 서버에서 메모리 사용량이 증가하거나 OOM이 발생했을 때 어떤 지표와 로그를 확인해야 하는지 설명해 주세요.

답변: 먼저 free -h 나 vmstat으로 available 메모리, 캐시, swap 사용량 확인. linux는 남는 메모리를 페이지 캐시로 사용하므로 단순히 used값이 높다는 이유만으로 부족하다고 판단하지 않음. ps로 rss가 큰 프로세스를 찾고 시간에 따라 계속 증가하는지 확인한 뒤, dmesg나 journalctl에서 OOM Killer의 대상과 발생시점 확인. 컨테이너라면 실제 사용량과 메모리 limit도 함께 보고, 누수인지 제한값 문제인지 구분

참고 자료:

---

## [ROLE-006] 디스크 사용률이 100%에 도달했을 때 용량, inode, 삭제된 파일을 점유 중인 프로세스, 로그 증가 여부를 어떻게 확인할지 설명해 주세요.

답변: df -h로 파일 시스템 용량을 보고 df -i로 inode 고갈 여부를 따로 확인. 용량 문제라면 du로 큰 디렉터리를 좁히고, 로그, 임시 파일, 컨테이너 이미지 중 무엇이 증가했는지 찾는다. 파일을 삭제했는데 공간이 반환되지 않았다면 lsof +L1로 삭제된 파일 디스크립터를 계속 잡고 있는 프로세스를 확인한다. 원인 프로세스와 보존 정책을 먼저 파악한 뒤 정리하고 재발 방지를 위해 로그 로테이션, 디스크 알림 점검

참고 자료:

---

## [ROLE-007] 리눅스 서버에서 특정 외부 API에 접속할 수 없을 때 DNS, Route, Port, TCP 연결, HTTP 응답 순서로 점검하는 방법을 설명해 주세요.

답변: 먼저 dig이나 nslookup으로 도메인이 의도한 IP로 해석되는지 확인. 다음으로 ip route get 등을 이용해 해당 목적지로 나가는 인터페이스, 게이트웨이를 보고 클라우드 환경이라면 NAT, peering, 보안그룹 경로도 대조. 이후 nc나 telenet으로 포트 연결을 확인하고 curl -v로 TCP연결, TLS협상, HTTP 상태 코드를 단계별로 본다. 이렇게 하면 DNS문제인지, 네트워크차단인지, 애플리케이션 응답 문제인지 구분 가능 

참고 자료:

---

## [ROLE-008] `Connection Timeout`, `Connection Refused`, DNS 오류, HTTP 5xx 오류의 차이와 각각의 발생 지점을 설명해 주세요.

답변: DNS오류는 도메인을 목적지 IP로 바꾸는 단계에서 실패한 것. Conneciton Timeout은 SYN에 응답이 없거나 중간에서 패킷이 드롭된 경우가 많아 라우팅, 방화벽, 보안그룹을 우선 확인한다. Conneciton refused는 목적지까지는 도달했지만 해당 포트에 리스닝 중인 포트가 없거나 즉시 거부한 경우. HTTP 5xx는 TCP와 HTTP 연결은 성립했고 서버 또는 상위 프록시가  요청 처리 중 실패한 것이므로 애플리케이션 로그와 하위 의존성 확인

참고 자료:

---

## [ROLE-009] Terraform 코드를 변경할 때 기존 리소스의 의도치 않은 삭제를 방지하기 위해 어떤 검증 절차를 거쳐야 하는지 설명해 주세요.

답변: 저는 기존 Terraform 구조를 수정할 때 먼저 대상 환경과 state가 맞는지 확인하고 fmt, validate, plan 순서로 검증합니다. 특히 plan에서 생성 변경 대상뿐아니라 destroy 0인지, 삭제나 교체나 포함됐는지를 확인하고 예상하지 못한 변경이 있으면 apply하지 않았습니다. 의존 리소스와 영향 범위도 함께 검토한 뒤 승인된 결과만 반영했습니다. 적용 후에는 콘솔 상태만 보지 않고 Ingress 주소나 실제 통신 결과 등 요청 목적이 달성됐는지 검증

참고 자료:

---

## [ROLE-010] Kubernetes에서 Pod가 어떤 Node에 배치되는지 설명하고, Resource Request와 실제 사용량이 스케줄링에 어떻게 반영되는지 설명해 주세요.

답변: Scheduler는 아직 node가 정해지지 않은 파드를 대상으로 request를 수용할 여유가 있는 Node를 필터링하고, affinity, taint와 toleration, topology 조건 등을 반영해 배치한다. 이 때 기본 스케줄링 기준은 실시간 사용량이 아니라, 선언된 cpu, memory request이다. limit은 배치 기준이라기보다 실행 중 사용할  수 있는 상한으로, cpu는 throttling되고 메모리는 초과 시 OOMKilled 될 수 있다. 따라서 request가 실제 사용량과 너무 다르면 과밀 배치나 불필요한 노드 증설이 발생할 수 있다.

참고 자료:

---

## [ROLE-011] Kubernetes Pod가 `Pending`, `CrashLoopBackOff`, `Running but NotReady` 상태일 때 각각 어떤 순서로 원인을 확인할지 설명해 주세요.

답변: 공통적으로 kubectl describe pod의 events와 kubectl logs를 먼저 확인한다. pending이면 request를 수용할 node가 있는지, taint, affinity, pvc,이미지 문제가 있는지 본다. crashloopbackoff라면 이전 컨테이너 로그와 종료 코드로 애플리케이션 오류, 설정 누락, OOM 여부 확인. running but notready라면 readiness probe의 경로, 포트 초기 지연과 의존 서비스 연결을 확인하고, 수정 후에는 재시작 횟수와 service endpoint 등록 여부까지 검증

참고 자료:

---

## [ROLE-012] EKS에서 별도 VPC의 Elastic Beanstalk API로 요청했을 때 Connection Timeout이 발생한 사례를 DNS 해석부터 보안그룹까지 순서대로 설명해 주세요.

답변: 먼저 보안그룹 아웃바운드, vpc peering, route table을 확인했지만 모두 정상이었습니다. 그래서 eks워커노드에 접속해 dig를 실행했고 목적지 도메인이 사설 ip가 아닌 공인 ip로 해석되는 것을 확인했습니다. 패킷은 peering이 아니라 nat gateway를 거치면서 출발지가 nat 공인 ip로 바뀌었고 목적지 보안그룹은 eks 사설 대역만 허용해 이를 차단하고 있었습니다. nat ip 두 개를 /32로 허용한 뒤 curl로 tcp연결과 http 308응답까지 확인했습니다.

참고 자료:

---

## [ROLE-013] 전사 앱과 웹이 모두 접속되지 않는 장애에서 Pod와 Node가 정상이라면, 어떤 근거로 EKS 내부를 제외하고 ALB·CDN·WAF·DNS 구간으로 범위를 좁힐 수 있는지 설명해 주세요.

답변: 당시 유저향 서비스 cpu가 동시에 급락했지만 pod 재시작은 없었고 node 메트릭과 내부 전용 서비스는 정상이었다. 부하로 처리에 실패했다면 cpu가 상승했을 가능성이 큰데, 일감 자체가 사라져 요청이 클러스터까지 오지 않았다고 판단했다. 이후 alb 지표와 dns 변경 이력을 확인해 정상 범위임을 소거하며 CDN,보안 영역까지 좁혔다. 최종 원인 확정은 다른 팀원이 했고, 나는 독립적 병렬 추적으로 앞단 보안 영역을 지목한 범위까지 기여했다.

참고 자료:

---

## [ROLE-014] 공통 EKS 클러스터를 무중단으로 전환할 때 작업 전, 작업 중, 작업 후에 각각 무엇을 확인해야 하는지 실제 경험을 기반으로 설명해 주세요.

답변: 작업 전에는 영향 범위와 롤백 기준을 공유하고, 계획된 단절이 장애로 오인되지 않도록 모니터링을 일시 중지함. 나는 새벽 공통 클러스터 전환에서 observability 스택의 도메인 8개를 전환하고 검증을 담당함. 전환 후에는 grafana에서 메트릭, 로그, 트레이스가 현재 시점에도 계속 수집되는지 클러스터별로 확인함. 특히 datasource와 time range가 맞는지 확인한 뒤 모니터링을 다시 활성화했고, 알림이 없다는 사실이 아니라 데이터가 실제로 돌아온 것을 완료 기준으로 삼음

참고 자료:

---

## [ROLE-015] 대규모 프로모션을 앞두고 인프라 용량을 산정할 때 어떤 지표를 확인해야 하며, 쿠폰 API 증설 판단이 빗나간 원인과 개선 방법을 설명해 주세요.

답변: 프로모션 전에는 예상 요청량과 이전 행사 최대값, cpu, memory, latency, asg 최대 수량과 target health를 함께 확인해야 한다. 당시 전날 트래픽이 적다는 이유로 쿠폰 API의 증설량을 줄였는데, 행사 마지막 날 트래픽이 집중되며 쿠폰 노출 오류가 발생했고, 개발팀 증설 후 안정화됐다. 이 사례를 통해 전날 수치 하나만 미래에 적용하면 안 된다는 점을 배웠다. 이후에는 요일, 행사 단계 같은 변수와 보수적인 여유 용량을 함께 반영해야 한다고 정리했다.

참고 자료:

---

## [ROLE-016] Smart RI Calc의 전체 구조와 업무 흐름을 설명하고, AI가 담당하는 부분과 코드가 담당하는 부분을 분리한 이유를 설명해 주세요.

답변: smart ri calc는 아키리뷰 회의록에서 필요한 인프라 사양을 읽고, ri 비용과 eks 노드 증설 예측을 자동화하는 도구입니다. ai는 비정형 문서에서 서비스, 사양을 추출하고, 담당자가 화면에서 결과를 검수, 수정하도록 했습니다. 가격 조회와 노드 계산은 aws pricing api와 규칙기반코드가 담당합니다. 언어 해석은 ai가 유리하지만 금액과 자원 계산은 같은 입력에 항상 같은 결과가 필요해 분리했고, 작업 시간을 약 20분에서 2분으로 줄였습니다.

참고 자료:

---

## [ROLE-017] Smart RI Calc에서 EKS 노드 증설 수를 예측한 방식을 설명하고, 실제 증설 결과와 7건 연속 일치했다는 결과를 어떻게 검증했는지 설명해 주세요.

답변: 현재 노드의 cpu, memory 잔여량과 추가될 파드의 request를 입력으로 받아 두 자원이 모두 들어가는 경우에만 기존 노드에 배치하는 규칙 기반 bin packing을 적용했다. 동일 앱 pod가 여러 개라면 HA와 최소 가용분을 고려해 /2를 한 후 반올림을 하여 배치하는 식으로, 최소로 살아남을 수 있는 파드 기준으로 RI를 구매하기 위해 노력했습니다.이후 실제 담당자가 증설한 결과와 건별로 대조했고 7건 연속 일치했습니다. 

참고 자료:

---

## [ROLE-018] Alert Adviser가 Slack 알림을 받은 뒤 Prometheus, Loki, Tempo의 데이터를 조회해 원인 후보를 제시하기까지의 흐름을 설명해 주세요.

답변: Alert adviser은 Slack의 Grafana 알림을 감지해 서비스와 알림 시각 같은 정보를 정규화합니다. 이후 조회 전용 Grafana 도구를 통해 Prometheus의 현재값과 24시간 기준값, Loki로그, Tempo 트레이스를 조회하고 필요한 경우 배포 시점도 함께 비교합니다. 분석 결과는 관측 사실과 원인 후보, 추가 확인 항목으로 구조화해 slack에 게시합니다.알림 문구만 보고 추측하는 것이 아니라 실제 관측 데이터를 근거로 조사 시작점을 줄이는 것이 핵심입니다.

참고 자료:

---

## [ROLE-019] Alert Adviser가 잘못된 원인을 확정적으로 제시하지 않도록 어떤 가드레일을 적용했으며, 변경성 명령어와 조직적 판단을 차단한 이유를 설명해 주세요.

답변: 한 장애에서 AI 가 DB 쿼리를 원인으로 지목했지만 실제 원인은 외부 검색 요청을 순차 호출한 로직이었던 사례가 있었습니다. 이후 근거 출처와 확신도를 분리하고, 모델이 표시한 값도 코드에서 다시 검사해 관측과 추측을 구분했습니다. 그라파나 도구도 조회 전용에만 허용하고 kubectl delete 나 scale같은 명령어는 출력에서 제거했습니다. 원인 후보는 자동화할 수 있지만 롤백, 장애 등급 에스컬레이션처럼 시스템과 조직에 영향을 주는 판단은 담당자가 해야 한다고 생각했습니다.

참고 자료:

---

## [ROLE-020] handDoc을 AWS EC2 기반 구조에서 Naver Cloud Kubernetes Service 기반 구조로 마이그레이션한 이유와 아키텍처 변경 사항을 설명해 주세요.

답변: handDoc은 처음에 EC2 한 대에서 spring boot와 fastapi를 도커로 실행하고 nginx가 TLS종료와 경로별 프록시를 담당했습니다. K-PaaS 공모전 제출을 위해 이를 NKS로 옮기면서 두 애플리케이션을 각각 deployment와 서비스로 분리하고 nginx ingress에서 /api, /fastapi, websocket경로를 라우팅했습니다. 기존 RDS와 MongoDB Atlas, Vercel 프론트는 유지했습니다. 단순히 컨테이너를 옮기는 데에서 끝나지 않고 probe와 외부 DB허용, CORS, WebSocket경로까지 Kubernetes환경에 맞게 다시 검증했습니다.

참고 자료:

---

## 선택 질문

## [ROLE-021] Nginx가 Reverse Proxy로 동작하는 원리를 설명하고, 일반 HTTP 요청과 WebSocket 요청의 프록시 설정이 어떻게 다른지 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-022] handDoc의 FastAPI 서버에서 AI 모델 초기화가 끝나기 전에 헬스체크가 시작되면 어떤 문제가 발생하며, Startup Probe를 어떻게 설정해야 하는지 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-023] GitLab CI, CodeDeploy, Argo CD가 각각 CI/CD 과정에서 어떤 역할을 하며, 명령형 배포와 GitOps 방식은 어떻게 다른지 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-024] IAM 권한 요청을 처리할 때 최소 권한 원칙을 어떻게 적용할 수 있으며, 기존 권한을 함께 검토해야 하는 이유를 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-025] EKS처럼 개별 Node IP가 변경될 수 있는 환경에서 보안그룹 Source를 단일 IP 대신 Prefix List나 Subnet 대역으로 관리하는 이유를 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-026] NAT Gateway, Internet Gateway, VPC Peering, Route Table, Security Group이 패킷 전달 과정에서 각각 어떤 역할을 하는지 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-027] 메트릭, 로그, 트레이스가 각각 장애 분석에 제공하는 정보가 무엇인지 설명하고, 하나의 요청 지연 원인을 세 데이터를 이용해 추적하는 과정을 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-028] Grafana Alert의 임계값을 설정할 때 평균, P95, 개별 호출 지연 중 어떤 값을 선택할지 판단하는 기준과 알림 노이즈를 줄이는 방법을 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-029] Kubernetes Secret 값이 변경됐는데 기존 Pod가 재시작되지 않아 컨테이너 간 인증값이 달라질 수 있는 이유와 이를 방지하는 방법을 설명해 주세요.

답변:

참고 자료:

---

## [ROLE-030] Argo CD 오픈소스에서 지원하지 않는 kubectl REST 플래그가 CLI Help에 노출되는 문제를 어떤 코드 흐름으로 분석하고 수정했는지 설명해 주세요.

답변:

참고 자료:

---